using Gtk 4.0;
template $AikadmWindow : Gtk.Window {
    can-focus: true;
    title: "Aikadm";
    Overlay  {
        hexpand: true;
        vexpand: true;
        [overlay]
        Picture background {  }
        [overlay]
        Label name {
           label: "---------";
        }
    }
}